@model IEnumerable<Library_Management_System_By_Nausif_AND_Mohsin.Models.Member_Type>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Member_Type_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Member_Discount)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Member_Type_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Member_Discount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Member_Type_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Member_Type_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Member_Type_ID })
        </td>
    </tr>
}

</table>
