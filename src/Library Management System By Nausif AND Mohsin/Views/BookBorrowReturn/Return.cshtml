@model Library_Management_System_By_Nausif_AND_Mohsin.Models.ReturnBookModels

@{
    ViewBag.Title = "Return";
}

<h2>Return</h2>


@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>ReturnBookModels</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.Book_Accessions, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownListFor(model => model.Book_Accessions, (SelectList)ViewBag.Book_Acc_Code, "Select Accession", new { required = "required" })
                @Html.ValidationMessageFor(model => model.Book_Accessions)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
